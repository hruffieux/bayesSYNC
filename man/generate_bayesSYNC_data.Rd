% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{generate_bayesSYNC_data}
\alias{generate_bayesSYNC_data}
\title{Simulate multivariate longitudinal data from the bayesSYNC model}
\usage{
generate_bayesSYNC_data(
  N,
  n,
  p,
  Q,
  L,
  mu_func,
  time_obs = NULL,
  n_g = 1000,
  a_om = 1,
  b_om = p,
  vec_sd_zeta = NULL,
  vec_sd_eps = NULL,
  seed = NULL
)
}
\arguments{
\item{N}{Number of subjects.}

\item{n}{Integer vector of length \code{N} giving the number of observation
times per subject.}

\item{p}{Number of variables (e.g., biomarkers).}

\item{Q}{Number of latent factors. Must be at most 4 (see
\code{\link{construct_splines}}).}

\item{L}{Number of FPCA components per factor. Must be at most 4.}

\item{mu_func}{Mean function with signature \code{mu_func(t, j, p)} returning
the mean of variable \code{j} at times \code{t}.}

\item{time_obs}{Optional list of length \code{N} of observation-time vectors.
If \code{NULL}, times are drawn uniformly on [0, 1].}

\item{n_g}{Size of the dense grid on which the true mean and eigenfunctions
are evaluated.}

\item{a_om, b_om}{Shape parameters of the Beta prior on the factor inclusion
probabilities \code{omega}. The default \code{b_om = p} yields
sparse loadings; lower \code{b_om} to make more variables load
on each factor.}

\item{vec_sd_zeta}{Vector of length \code{L} of score standard deviations per
component. Defaults to \code{1/(1:L)}.}

\item{vec_sd_eps}{Measurement-noise standard deviation, a scalar or a vector
of length \code{p}. Defaults to 1 for every variable.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
A named list with, among others: \code{time_obs}, \code{Y} (the data
        in \code{\link{bayesSYNC}} input format), the true loadings \code{B},
        inclusion indicators \code{gamma}, scores \code{Zeta} (list of
        \code{Q} matrices of size \code{N x L}), eigenfunctions \code{Phi} and
        their dense-grid version \code{Phi_g} (list of \code{Q} matrices of
        size \code{n_g x L}), and the mean functions \code{mu} and \code{mu_g}.
}
\description{
Generates high-dimensional curves following the bayesSYNC generative model:
a shared set of latent factors, each with subject-specific FPCA scores and
spline eigenfunctions, combined through variable-specific sparse loadings and
added to variable-specific mean functions, with Gaussian measurement noise.
The output is in the exact format expected by \code{\link{bayesSYNC}} and the
simulated ground truth can be aligned to the estimates with
\code{\link{match_factor_and_sign}}.
}
\seealso{
\code{\link{bayesSYNC}}, \code{\link{match_factor_and_sign}},
         \code{\link{construct_splines}}
}
